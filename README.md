# QuackTrack Habit Tracker

QuackTrack is a single-page habit tracker built with plain HTML, CSS, and JavaScript.

## Run Locally

No installs are needed.

1. Download or clone the project.
2. Open `index.html` in any modern browser.

The app stores habits and checkmarks in `localStorage`, so your data stays after a page reload in the same browser.
